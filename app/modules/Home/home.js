'use strict';

angular.module('Home', ['ngRoute'])
    .controller('HomeController', function () {
        // $scope.userLogin = userService.getUser();
        // // $scope.isLogin = user.isLogin;
        // // $scope.userInfo = user.userInfo;
    });